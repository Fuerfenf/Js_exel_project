Webpack (import/export)
    babel <- cross new/old versions  +
    scss <- preprocessor +
    2 Modes
        - Dev
            Dev Server, +
            SourceMap (only moduls factoring), +
            Eslint, <- clear code +
            Not Minified js & CSS +
        - Prod
            Minified Code +
Git

Class module starts on uppercase, dif module with functions stroke case
