export class Excel {
    constructor(selector, options) {
        this.$el = document.querySelector(selector) // marks $ DOM node
        this.components = options.components || [] // in base free array
    }
    get


    render() {
        // // chanje elem  (afterbegin, afterend, beforeend, beforebegin)
        // this.$el.insertAdjacentHTML('afterbegin',) //chanje in html
    }
}