export {defaultStyles, defaultTitle};

const defaultStyles = {
    textAlign: 'left',
    fontWeight: 'normal',
    textDecoration: 'none',
    fontStyle: 'normal',
};
const defaultTitle = 'New Table';